<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">
	<!-- 카테고리 내 게시글 목록 조회 -->
	<select id="findPostsByCategoryId" resultType="postvo" parameterType="long">
		<choose>
			<when test="categoryId == null">
				<![CDATA[
					select id, title, contents, reg_date as regDate, category_id as categoryId from post
				]]>
			</when>
			<otherwise>
				<![CDATA[
					select id, title, contents, reg_date as regDate, category_id as categoryId from post where category_id=#{categoryId}
				]]>
			</otherwise>
		</choose>
	</select>
	
	<!-- 게시글 조회 -->
	<select id="findPostById" resultType="postvo">
		<![CDATA[
			select id, title, contents, reg_date as regDate from post where id=#{postId}
		]]>
	</select>
	
	<!-- 게시글 작성 -->
	<insert id="insertPost" parameterType="postvo">
		<![CDATA[
			insert into post values(null, #{title}, #{contents}, now(), #{categoryId})
		]]>
	</insert>
	
	<!-- 카테고리 목록 조회 -->
	<select id="selectCategories" resultType="categoryvo">
		<![CDATA[
			select id, name, description from category where blog_id=#{blogId};
		]]>
	</select>
	
	<!-- 카테고리 추가 -->
	<insert id="insertCategory" parameterType="categoryvo">
		<![CDATA[
			insert into category values(null, #{name}, #{description}, #{blogId})
		]]>
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			<![CDATA[
				select last_insert_id() from dual
			]]>
		</selectKey>
	</insert>
	
	<!-- 관리자 블로그 정보 조회 -->
	<select id="findBlogById" resultType="blogvo">
		<![CDATA[
		select title, profile from blog where blog_id=#{blogId};
		]]>
	</select>
	
	<!-- 관리자 블로그 정보 수정 -->
	<update id="updateBlog" parameterType="blogvo">
		<![CDATA[
		update blog set title=#{title}, profile=#{profile} where blog_id = #{id}
		]]>
	</update>
</mapper>
